import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    paddingHorizontal: 4,
  },
  avatar: {
    width: 59,
    height: 59,
    borderRadius: 999,
  },
  name: {
    marginTop: 8,
    color: 'white',
    fontSize: 12,
    textAlign: 'center',
    lineHeight: 16,
    maxWidth: 70,
  },
});

export default styles;
